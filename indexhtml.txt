<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AI Video Marketing</title>

  <!-- SEO -->
  <meta name="description" content="AI-powered video marketing, reels, stories and digital promotions for modern brands." />

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --bg: #0f1115;
      --bg-soft: #151822;
      --text: #e8e8ea;
      --muted: #a0a3b1;
      --accent: #6cf2c2;
      --accent-dark: #4ccfa4;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.6;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    header {
      position: sticky;
      top: 0;
      z-index: 100;
      background: rgba(15, 17, 21, 0.9);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid #1f2330;
    }

    .nav {
      max-width: 1100px;
      margin: auto;
      padding: 1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-weight: 700;
      letter-spacing: 0.5px;
    }

    .nav a {
      margin-left: 1.2rem;
      font-size: 0.9rem;
      color: var(--muted);
    }

    .nav a:hover {
      color: var(--text);
    }

    section {
      max-width: 1100px;
      margin: auto;
      padding: 4rem 1.2rem;
    }

    .hero {
      min-height: 90vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .hero h1 {
      font-size: clamp(2.2rem, 5vw, 3.5rem);
      margin-bottom: 1rem;
    }

    .hero p {
      color: var(--muted);
      max-width: 600px;
      margin-bottom: 2rem;
    }

    .btn {
      display: inline-block;
      padding: 0.9rem 1.6rem;
      background: var(--accent);
      color: #000;
      font-weight: 600;
      border-radius: 6px;
      transition: background 0.2s ease;
    }

    .btn:hover {
      background: var(--accent-dark);
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 1.5rem;
      margin-top: 2rem;
    }

    .card {
      background: var(--bg-soft);
      padding: 1.5rem;
      border-radius: 10px;
      border: 1px solid #1f2330;
    }

    .card h3 {
      margin-bottom: 0.6rem;
    }

    .card p {
      color: var(--muted);
      font-size: 0.9rem;
    }

    footer {
      border-top: 1px solid #1f2330;
      text-align: center;
      padding: 2rem 1rem;
      color: var(--muted);
      font-size: 0.85rem;
    }

    /* Mobile tweaks */
    @media (max-width: 600px) {
      .nav {
        flex-direction: column;
        gap: 0.6rem;
      }

      .nav a {
        margin: 0 0.6rem;
      }
    }
  </style>
</head>
<body>

<header>
  <nav class="nav">
    <div class="logo">AI Video Marketing</div>
    <div>
      <a href="#services">Services</a>
      <a href="#about">About</a>
      <a href="#contact">Contact</a>
    </div>
  </nav>
</header>

<section class="hero">
  <h1>AI-Powered Video Marketing</h1>
  <p>
    We create short-form AI videos, reels and digital content that help brands
    stand out on social media — fast, modern and cost-effective.
  </p>
  <a href="#contact" class="btn">Get in touch</a>
</section>

<section id="services">
  <h2>Services</h2>
  <div class="grid">
    <div class="card">
      <h3>AI Video Ads</h3>
      <p>Short promotional videos optimized for Instagram, TikTok and ads.</p>
    </div>
    <div class="card">
      <h3>Reels & Stories</h3>
      <p>Vertical content designed to grab attention in seconds.</p>
    </div>
    <div class="card">
      <h3>Product Showcases</h3>
      <p>Clean, elegant videos presenting materials, interiors and products.</p>
    </div>
  </div>
</section>

<section id="about">
  <h2>About</h2>
  <p style="max-width: 700px; color: var(--muted); margin-top: 1rem;">
    We combine AI tools with visual storytelling to create modern marketing
    content for small and medium businesses. No agencies. No complexity.
    Just effective digital presence.
  </p>
</section>

<section id="contact">
  <h2>Contact</h2>
  <p style="margin-top: 1rem; color: var(--muted);">
    Email: <strong>ai.video.marketing.rs@gmail.com</strong><br>
    Instagram: <strong>@ai.video.marketing.rs</strong>
  </p>
</section>

<footer>
  © 2026 AI Video Marketing. All rights reserved.
</footer>

</body>
</html>
